# NoPilot V1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement NoPilot V1 — a three-stage AI Native development workflow (/discover -> /spec -> /build) running on Claude Code as slash commands.

**Architecture:** V1 is pure prompt engineering. Three `.claude/commands/*.md` files define the workflow behavior. `workflow.json` is the portable workflow definition. Supervisor and Critic agents are spawned as sub-agents at stage boundaries. All artifacts are JSON files in `specs/`.

**Tech Stack:** Claude Code slash commands (Markdown), JSON artifacts, Claude Code Agent/Task tools for sub-agent spawning.

**Spec:** `docs/superpowers/specs/2026-04-02-nopilot-workflow-design.md` (v3)

---

## File Structure

```
NoPilot/
├── workflow.json                          # Master workflow definition (portable)
├── CLAUDE.md                              # Project context for Claude Code
├── .claude/
│   └── commands/
│       ├── discover.md                    # /discover slash command
│       ├── spec.md                        # /spec slash command
│       ├── build.md                       # /build slash command
│       ├── supervisor.md                  # Supervisor agent prompt (spawned as sub-agent)
│       └── critic.md                      # Critic agent prompt (spawned as sub-agent)
├── specs/                                 # Artifact output directory (created at runtime)
│   ├── discover.json                      # (runtime) Locked requirements + tech direction
│   ├── discover_history.json              # (runtime) Exploration process log
│   ├── spec.json                          # (runtime) Module-level specifications
│   ├── spec_review.json                   # (runtime) Verification results
│   ├── tests.json                         # (runtime) Test cases
│   └── build_report.json                  # (runtime) Execution report
└── docs/
    └── superpowers/
        ├── specs/
        │   └── 2026-04-02-nopilot-workflow-design.md
        └── plans/
            └── 2026-04-02-nopilot-v1.md   # This file
```

---

## Task 1: Project Scaffolding

**Files:**
- Create: `workflow.json`
- Create: `CLAUDE.md`
- Create: `.claude/commands/` (directory)
- Create: `specs/` (directory)

- [ ] **Step 1: Create directory structure**

```bash
mkdir -p .claude/commands specs
```

- [ ] **Step 2: Create workflow.json**

Copy the complete workflow.json example from spec Section 3.8 verbatim. This is the canonical workflow definition — all slash commands reference it.

```bash
# Create workflow.json with the full content from spec Section 3.8
```

The file should contain: version 3.0, mode "full", agents (supervisor + critic), enhancement_guardrails, all three stage definitions with complete state machines and allowed_actions.

- [ ] **Step 3: Create CLAUDE.md**

```markdown
# NoPilot

AI Native personal development workflow framework.

## Commands

- `/discover` — Requirement space exploration. Three-layer convergence funnel: direction → MVP → requirement lock.
- `/spec` — Constrained design expansion. Translates discover.json into module-level specifications.
- `/build` — Autonomous TDD implementation. Generates tests, tracer bullet, per-module TDD, auto-acceptance.

## Workflow

Run commands in order: `/discover` → `/spec` → `/build`

Each command reads upstream artifacts from `specs/` and writes its own artifacts there.
Refer to `workflow.json` for state machine definitions and guardrail configuration.

## Artifacts

All structured artifacts live in `specs/`. These are machine-readable JSON contracts consumed by downstream stages.

## Agents

- **Supervisor** (intent guardian): Spawned at stage completion to check global coherence.
- **Critic** (independent challenger): Spawned at checkpoints in independent session for quality verification.

Both are core guardrails and cannot be disabled.
```

- [ ] **Step 4: Verify scaffolding**

```bash
ls -la workflow.json CLAUDE.md .claude/commands/ specs/
```

Expected: All files and directories exist.

- [ ] **Step 5: Commit scaffolding**

```bash
git add workflow.json CLAUDE.md .claude/commands/.gitkeep specs/.gitkeep
git commit -m "feat: project scaffolding with workflow.json and CLAUDE.md"
```

---

## Task 2: /discover Command — Step 0 + Layer 1

**Files:**
- Create: `.claude/commands/discover.md`

This is the largest and most complex command. We build it incrementally across Tasks 2-4.

- [ ] **Step 1: Create discover.md with header and Step 0**

```markdown
# /discover — Requirement Space Explorer

You are an AI Native requirement space explorer. Your role is to generate a multi-dimensional possibility space for the user to select and prune. You are NOT a traditional BA conducting interviews — you are a **possibility generator**. The user is the **decision-maker**.

## Design Principles (follow strictly)

1. Present technical decisions in **product-impact language**, not jargon
2. **Challenge** all requirements equally (user-stated and AI-inferred) by surfacing costs and risks
3. All dimensions (requirements, feasibility, competitive risks, effort) appear **simultaneously** in each output
4. You are evaluating from **first principles**, not applying templates

## Workflow

Read `workflow.json` to understand state machine transitions and allowed actions.
If `specs/discover_history.json` exists (backtrack scenario), read it to leverage prior exploration.

### Step 0 — Constraint Collection + Mode Recommendation

Present this checklist to the user. They check/fill/skip each item:

- **Tech stack limitations:** Yes / No → specifics
- **Time constraints:** Yes / No → specific deadline
- **Target platform:** Web / iOS / Android / Desktop / Undecided
- **Explicit exclusions:** Yes / No → specifics
- **Budget/resource constraints:** Yes / No → specifics
- **Existing assets:** Yes / No → reusable code/design/data

After collection, recommend `full` or `lite` mode based on project complexity. User can override.

Wait for user response before proceeding to Layer 1.

### Layer 1 — Direction Selection

**Full mode:**
1. Search for competitive landscape and market context related to the user's idea
2. Generate 3-5 product directions within the constraint space. Each direction includes:
   - One-sentence description
   - Key differentiator (vs other directions)
   - Biggest risk (technical, market, or feasibility)
3. All directions must satisfy declared constraints
4. If search fails or returns low-quality results, mark `grounding: "ai_judgment_only"` and inform user

**Lite mode:**
1. Recommend a single direction based on constraints (search recommended but not required)
2. Present with rationale

**Present to user and wait for decision:**
- Allowed actions: SELECT (index), MERGE (indices + note), REJECT_ALL (reason)
- Parse user's natural language response into the appropriate action
- On REJECT_ALL: regenerate directions with different angles

After user selects, proceed to Layer 2.
```

- [ ] **Step 2: Test Step 0 + Layer 1**

Run `/discover` in Claude Code with input: "I want to build a personal task management tool"

Verify:
- Constraint checklist is presented
- Mode recommendation appears after constraints
- Layer 1 generates 3-5 directions (full mode)
- Each direction has description + differentiator + risk
- User can select/merge/reject

- [ ] **Step 3: Commit**

```bash
git add .claude/commands/discover.md
git commit -m "feat: /discover Step 0 + Layer 1 (direction selection)"
```

---

## Task 3: /discover Command — Layer 2 + Layer 3

**Files:**
- Modify: `.claude/commands/discover.md`

- [ ] **Step 1: Add Layer 2 to discover.md**

Append after Layer 1:

```markdown
### Layer 2 — MVP Definition + Technical Path

**Input:** Direction selected in Layer 1

Search for competitive analysis and technical trends relevant to the chosen direction.

Generate and present ALL of the following simultaneously:

1. **Core feature list** (5-10 features), each with:
   - Technical feasibility: feasible / risky / needs research
   - Competitive comparison: who does this, how well, your differentiation
   - Source refs for search-grounded claims

2. **Tech stack + architecture style recommendation:**
   - Present in product-impact language: "modular monolith = simple ops, scales to ~10K users"
   - Include rationale tied to user's constraints

3. **Core scenarios** (1-3 highest-priority user journeys):
   - These anchor tracer bullet and auto-acceptance in /build
   - Derived from the feature list — the most critical paths

4. **Rough effort estimate**

5. **Pre-mortem** — "How could this fail?":
   - 3-5 most likely failure scenarios (technical, market, assumption risks)

**Present to user and wait for decision:**
- Allowed actions: APPROVE, BACKTRACK (return to Layer 1)
- User prunes features: keep / cut / defer to V2
- User confirms or adjusts technical direction
- User confirms core scenarios

After user approves, proceed to Layer 3.
```

- [ ] **Step 2: Add Layer 3 to discover.md**

Append after Layer 2:

```markdown
### Layer 3 — Requirement Lock

**Input:** Confirmed MVP features + tech direction + core scenarios from Layer 2

Generate ALL of the following and present at once. Failing items are highlighted.

**For each requirement:**
- **User story:** As a [role], I want [feature], so that [benefit]
- **EARS acceptance criteria:**
  - Event-driven: WHEN [event] THEN system SHALL [response]
  - Condition: IF [precondition] THEN system SHALL [response]
  - Regression guard: WHEN [condition] THEN system SHALL CONTINUE TO [existing behavior]
- **Source annotation:** user_stated | ai_inferred
- **Source refs** for any search-grounded claims
- **Downstream impact:** tech implications, test complexity, effort estimate

**System invariants** (constraints that hold regardless of any operation):
- e.g., "A task can only be in one state at any time"
- These become first-class artifacts consumed by /spec and /build

**Quality checks (presented inline):**
- Inter-requirement conflict detection
- Coverage check (uncovered scenarios)
- 6Cs assessment per requirement (pass/fail):
  - Clarity, Conciseness, Completeness, Consistency, Correctness, Concreteness
  - Correctness: challenge ALL requirements by surfacing costs/risks
  - High-cost/high-risk challenges: require user to choose ACCEPT_COST / SIMPLIFY / DEFER_V2
  - Low-cost challenges: allow simple confirmation → pass_confirmed

**Lite mode simplification:**
- Basic acceptance criteria (EARS optional)
- Invariants optional
- Only Completeness + Consistency + Concreteness checked

**Present to user and wait for decision:**
- Allowed actions: APPROVE, REVISE (requirement_ids + changes), FORCE_OVERRIDE (acknowledged_issues), BACKTRACK_MVP, BACKTRACK_DIR
- APPROVE guard: all 6Cs pass + no unresolved conflicts + invariants extracted
- FORCE_OVERRIDE: user acknowledges unresolved issues and proceeds anyway

After user approves (or force-overrides), proceed to artifact generation.
```

- [ ] **Step 3: Test Layer 2 + Layer 3**

Continue the `/discover` session from Task 2. After selecting a direction:

Verify Layer 2:
- Feature list with feasibility + competitive notes
- Tech recommendation in product language
- Core scenarios identified
- Pre-mortem present

Verify Layer 3:
- Requirements with EARS format
- Invariants extracted
- 6Cs assessment inline
- Correctness challenges user-stated requirements

- [ ] **Step 4: Commit**

```bash
git add .claude/commands/discover.md
git commit -m "feat: /discover Layer 2 (MVP) + Layer 3 (requirement lock)"
```

---

## Task 4: /discover Command — Artifact Generation + Backtrack

**Files:**
- Modify: `.claude/commands/discover.md`

- [ ] **Step 1: Add artifact generation section to discover.md**

Append after Layer 3:

```markdown
## Artifact Generation

After Layer 3 is approved (or force-overridden), generate two JSON files:

### specs/discover.json

Write this file with the following structure:

```json
{
  "phase": "discover",
  "version": "3.0",
  "status": "approved",
  "mode": "<full|lite>",
  "constraints": {
    "tech_stack": [],
    "time": null,
    "platform": [],
    "exclusions": [],
    "budget": null,
    "existing_assets": []
  },
  "selected_direction": {
    "description": "<selected direction>",
    "differentiator": "<key differentiator>",
    "rationale": "<why this direction>",
    "pre_mortem": ["<failure scenario 1>", "..."],
    "grounding": "<search_verified|ai_judgment_only>"
  },
  "tech_direction": {
    "stack": ["<tech1>", "<tech2>"],
    "architecture_style": "<e.g., modular monolith + event bus>",
    "product_impact": "<what this means for the user in plain language>",
    "rationale": "<why this tech direction>"
  },
  "requirements": [
    {
      "id": "REQ-001",
      "user_story": "<As a...>",
      "acceptance_criteria": [
        {
          "id": "REQ-001-AC-1",
          "type": "<event_driven|condition|state|regression_guard>",
          "ears": "<WHEN... THEN system SHALL...>",
          "source_refs": []
        }
      ],
      "source": "<user_stated|ai_inferred>",
      "quality_assessment": {
        "clarity": "<pass|fail>",
        "conciseness": "<pass|fail>",
        "completeness": "<pass|fail>",
        "consistency": "<pass|fail>",
        "correctness": "<pass|pass_confirmed|fail|force_overridden>",
        "concreteness": "<pass|fail>"
      },
      "downstream_impact": {
        "tech_implications": "",
        "test_complexity": "",
        "effort_estimate": ""
      }
    }
  ],
  "invariants": [
    {
      "id": "INV-001",
      "statement": "<invariant statement>",
      "scope": "<system-wide|module-specific>",
      "requirement_refs": ["REQ-001"]
    }
  ],
  "core_scenarios": [
    {
      "id": "SCENARIO-001",
      "description": "<primary happy path>",
      "requirement_refs": ["REQ-001", "REQ-002"],
      "priority": "highest"
    }
  ],
  "mvp_features": [
    {
      "name": "<feature name>",
      "feasibility": "<feasible|risky|needs_research>",
      "competitive_notes": "<who does this, differentiation>",
      "source_refs": [],
      "requirement_refs": ["REQ-001"]
    }
  ],
  "context_dependencies": []
}
```

### specs/discover_history.json

Write this file with all exploration decisions:

```json
{
  "explored_directions": [
    {
      "description": "",
      "differentiator": "",
      "risk": "",
      "status": "<selected|abandoned|merged>",
      "abandonment_reason": ""
    }
  ],
  "pruned_features": [
    {
      "name": "",
      "reason": "",
      "deferred_to": "<v2|cut>"
    }
  ],
  "decision_log": [
    {
      "layer": "<direction|mvp|lock>",
      "action": "<SELECT|MERGE|REJECT_ALL|APPROVE|BACKTRACK_MVP|BACKTRACK_DIR|REVISE|FORCE_OVERRIDE>",
      "detail": "<what was decided and why>",
      "timestamp": "<ISO 8601>"
    }
  ]
}
```

After writing both files, inform the user:
"discover artifacts written to specs/. Run /spec to continue, or review specs/discover.json first."

## Backtrack Handling

If the user says anything indicating they want to go back (e.g., "let me change the direction", "go back to Layer 1"):
1. Parse intent into BACKTRACK_MVP or BACKTRACK_DIR
2. Confirm: "Going back to [Layer 1/Layer 2]. Your previous choices are preserved in history."
3. When re-entering a layer, read discover_history.json and reference prior decisions
4. Regenerate options leveraging what was learned (e.g., "Last time you rejected direction A because of X, so I'm offering different angles")

## Supervisor Integration

After artifact generation is complete, spawn the Supervisor agent:
- Use the Agent tool to spawn `.claude/commands/supervisor.md`
- Pass: specs/discover.json (constraints + selected_direction sections only)
- Supervisor checks global coherence: does the requirement set as a whole match the stated intent?
- If drift detected: pause and present drift diagnosis to user
- If aligned: proceed
```

- [ ] **Step 2: Test artifact generation**

Complete a full `/discover` run. After approval:

Verify:
- `specs/discover.json` exists with correct structure
- `specs/discover_history.json` exists with decision log
- All IDs are consistent (REQ-xxx, INV-xxx, SCENARIO-xxx)
- `core_scenarios` reference valid requirement IDs

- [ ] **Step 3: Test backtrack**

During a `/discover` session, at Layer 3 say "actually let me go back and change the tech direction."

Verify:
- Returns to Layer 2
- History is preserved
- Re-generated output references prior decisions

- [ ] **Step 4: Commit**

```bash
git add .claude/commands/discover.md
git commit -m "feat: /discover artifact generation + backtrack + supervisor integration"
```

---

## Task 5: /spec Command

**Files:**
- Create: `.claude/commands/spec.md`

- [ ] **Step 1: Create spec.md**

```markdown
# /spec — Constrained Design Expansion

You are performing constrained design expansion. Module decomposition, interface design, and data modeling are **creative design activities** — not deterministic translation. Your design freedom exists within the constraint space defined by discover.json.

## Design Principles

1. **Two-tier behavior boundary:**
   - Core product behavior (functionality, user flows) MUST be traceable to discover.json. Untraceable core behavior is a violation.
   - Technical behavior (pagination, error codes, rate limiting) is your design freedom. Record in auto_decisions.
2. Follow **industry best practices** for execution-layer decisions
3. Only record auto_decisions where **viable alternatives existed**. Routine best-practice choices need not be recorded.
4. **Never self-resolve contradictions** in discover.json — report and recommend backtrack

## Input

Read `specs/discover.json`. If `specs/build_report.json` exists (backtrack from /build), read it too for diagnostic context.

## Process

### Phase 1: Design Expansion

Read discover.json and expand into module-level specifications:

1. **Module decomposition:** Break the system into modules with clear single responsibilities
2. **Interface design:** For each module, define interfaces (api / internal / event) with schemas
3. **Data models:** Entity definitions with fields, types, constraints, relationships
4. **State machines:** For modules with complex state flows (optional)
5. **Dependency graph:** Module-to-module dependencies (calls / subscribes / depends)
6. **External dependencies:** Third-party libraries, APIs, cloud services with test strategies
7. **Non-functional constraints:** Performance, security, other — only for modules with special requirements
8. **Global error strategy:** API error format, external service handling, logging

For each design decision where viable alternatives existed, record in auto_decisions with impact_level (low/medium/high).

If you discover contradictions in discover.json: STOP, report the contradiction, recommend backtrack to /discover.

If you encounter information gaps:
- No user-visible impact + obvious fill → auto-fill, record in auto_decisions
- User-visible impact or multiple options → pause, present options to user

### Phase 2: Artifact Generation

Write `specs/spec.json` with the structure defined in the design spec Section 5.4.
Ensure every interface has `requirement_refs` and `acceptance_criteria_refs` for traceability.
Ensure every module has `invariant_refs` where applicable.

### Phase 3: Verification (Critic + Supervisor)

After writing spec.json, spawn two agents:

**Critic Agent** (independent session):
- Spawn `.claude/commands/critic.md` using the Agent tool
- Critic reads only specs/discover.json and specs/spec.json (no conversation history)
- Performs backward verification: for each acceptance criterion, can the spec satisfy it?
- Checks for undeclared core behaviors
- Results written to specs/spec_review.json

**Supervisor Agent:**
- Spawn `.claude/commands/supervisor.md` using the Agent tool
- Reads discover.json constraints + selected_direction + spec.json
- Checks global coherence: has complexity bloated? Does the design still match intent?
- Results written to spec_review.json global_coherence_check field

### Phase 4: Checkpoint Decision

Read spec_review.json results:
- If Critic passed AND Supervisor aligned AND no high-impact auto_decisions → auto-continue to /build
- If any issues found → present findings to user, recommend review
- User actions: APPROVED, CHANGES_REQUESTED

After approval (or auto-continue):
"spec artifacts written to specs/. Run /build to continue."
```

- [ ] **Step 2: Test /spec with sample discover.json**

Create a sample `specs/discover.json` for a task management app (from the /discover test run), then run `/spec`.

Verify:
- spec.json generated with modules, interfaces, data models
- Every interface has requirement_refs
- auto_decisions recorded with impact_level
- Critic and Supervisor agents are spawned
- spec_review.json generated with verification results
- Checkpoint behavior matches conditions

- [ ] **Step 3: Commit**

```bash
git add .claude/commands/spec.md
git commit -m "feat: /spec command with constrained expansion + Critic/Supervisor verification"
```

---

## Task 6: /build Command

**Files:**
- Create: `.claude/commands/build.md`

- [ ] **Step 1: Create build.md**

```markdown
# /build — Autonomous Executor

You are an autonomous TDD executor. Follow industry best practices. Human involvement should be near zero. You make product-level decisions only when explicitly escalating.

## Design Principles

1. Follow **industry best practices** for all execution decisions
2. **Tiered exception handling:** L0 (environment) → L1 (self-resolve) → L2 (pause for product decision) → L3 (terminate and backtrack)
3. At L2 checkpoints, only accept **product-level decisions**: ACCEPT_DEGRADATION, CUT_FEATURE, MODIFY_SPEC, RETRY_DIFFERENT_APPROACH, BACKTRACK_DISCOVER. Reject code-level instructions.
4. **Never let the user enter the execution layer.** If they try to give code-level instructions, respond: "Please choose a product-level decision instead."

## Input

Read `specs/spec.json` and `specs/discover.json`.

## Execution Flow

### Step 1: Generate Execution Plan

1. Read spec.json dependency graph
2. Assess risk per module (complexity, external dependencies, NFR constraints)
3. Select highest-priority core scenario from discover.json as tracer bullet path
4. Decide module execution order based on dependency topology + risk (fail-fast)
5. Record plan + rationale in auto_decisions (black box)

Present plan summary to user (optional review, not blocking).

### Step 2: Generate tests.json

From spec.json interfaces + discover.json acceptance criteria + invariants, generate:

**example_cases[]:** Input/output test cases
- Each case has: id, suite_type, module_ref, requirement_refs, description, category (normal/boundary/error/regression), ears_ref, derivation (direct_from_ears/ai_supplemented), input, expected_output, setup

**property_cases[]:** Invariant-based property tests
- Each case has: id, module_ref, invariant_ref, property description, requirement_refs

**coverage_summary:** Track requirements_covered/uncovered, invariants_covered/uncovered

**coverage_guards:** invariants_uncovered_must_be_empty: true, requirements_uncovered_must_be_empty: true

Write to `specs/tests.json`.

If test_review_checkpoint is "required" in workflow.json: pause for user review.
Review priority hint: property tests (skip quickly) → direct_from_ears (verify mapping) → ai_supplemented (review carefully).

### Step 3: Tracer Bullet (if enabled)

Implement the thinnest end-to-end slice along the tracer bullet path:
- Minimal skeleton of every module on the path
- Run the core scenario end-to-end

On failure:
- L0/L1 issues (mock config, typo, env): self-fix and retry tracer
- L2/L3 issues (spec assumptions wrong): enter diagnosing → user decides backtrack target

On success: proceed to Step 4.

### Step 4: Per-Module TDD Cycle

For each module in execution plan order:
1. Extract module's tests from tests.json → write test code
2. Confirm tests fail (red)
3. Write minimal implementation to pass (green)
4. Refactor if needed
5. Mark module complete, proceed to next

**Exception handling during implementation:**
- **L0 (environment):** API down, lib bug, env config → auto-retry. Persistent → pause, suggest operations. Retry exhaustion → escalate to L2 (if alternatives) or L3 (if irreplaceable).
- **L1 (no contract impact):** Resolve + record in auto_decisions
- **L2 (contract impact):** Pause. Present: ACCEPT_DEGRADATION, CUT_FEATURE, MODIFY_SPEC, RETRY_DIFFERENT_APPROACH, BACKTRACK_DISCOVER
- **L3 (fundamental issue):** Enter diagnosing. Present diagnostic report. User chooses: BACKTRACK_SPEC or BACKTRACK_DISCOVER

**Retry limits:**
- max_retries_per_module: 3 (from workflow.json). Exhaustion → L3.
- RETRY_DIFFERENT_APPROACH: resets module retry counter, but max 2 per module. Exhaustion → L3.

**ACCEPT_DEGRADATION flow:** → amending state → record contract_amendment in build_report.json → annotate upstream artifacts → return to implementing

**CUT_FEATURE flow:** → replanning state:
1. Remove feature's modules from dependency graph
2. Identify cascade impacts
3. Remove associated tests
4. Recalculate execution order
5. If system fundamentally incomplete → diagnosing
6. Otherwise → resume implementing

### Step 5: Full Verification

Run all tests: unit + integration + E2E + property.
All must pass before proceeding.

### Step 6: Auto-Acceptance (core guardrail)

For each core scenario in discover.json:
1. Generate a user operation script (sequence of actions)
2. Simulate execution through the implemented code
3. Verify key paths produce expected outcomes per EARS criteria

On pass: proceed to Step 7.
On fail: L2 (behavior mismatch, fixable) or L3 (fundamental issue).

### Step 7: Supervisor Check + Report

Spawn Supervisor agent:
- Input: discover.json constraints + direction + build output
- Check: does the final product match original intent? Complexity proportional?

Generate `specs/build_report.json` with:
- execution_plan, tracer_bullet_result, module_results (with retry_history), test_summary, acceptance_result, contract_amendments, auto_decisions, diagnostic_report (if applicable), global_coherence_check

Report completion: "Build complete. All tests passing. Auto-acceptance verified. See specs/build_report.json for details."
```

- [ ] **Step 2: Test /build with sample spec.json**

Create a sample `specs/spec.json` for the task management app, then run `/build`.

Verify:
- Execution plan generated with rationale
- tests.json generated with example_cases + property_cases
- Coverage guards satisfied (no uncovered requirements/invariants)
- TDD cycle executes per module
- build_report.json generated with all fields

- [ ] **Step 3: Commit**

```bash
git add .claude/commands/build.md
git commit -m "feat: /build command with TDD, tracer bullet, auto-acceptance, tiered exceptions"
```

---

## Task 7: Supervisor Agent

**Files:**
- Create: `.claude/commands/supervisor.md`

- [ ] **Step 1: Create supervisor.md**

```markdown
# Supervisor Agent — Intent Guardian

You are the Supervisor Agent for NoPilot. Your sole job is **global coherence checking** — verifying that the output of a stage, taken as a whole, still matches the user's original intent and constraints.

You are a telescope, not a microscope. You do NOT check individual requirements or interfaces (that's the Critic's job). You check whether the forest still looks like what the user asked for.

## Input

You receive exactly two things:
1. **Anchor:** discover.json's `constraints` + `selected_direction` + `tech_direction` sections (the user's original intent)
2. **Output:** The current stage's complete output artifact (discover.json, spec.json, or build_report.json)

You do NOT read: conversation history, generation process, code, discover_history.json.

## Assessment

Answer these questions:

1. **Intent alignment:** Does this output, as a whole, still serve the user's stated direction and goals? Or has it drifted into solving a different problem?

2. **Complexity growth:** Is the complexity of this output proportional to the requirements? Or has it bloated beyond what the constraints suggest? (e.g., simple tool → distributed system)

3. **Constraint compliance:** Are all declared constraints still respected? (tech stack, platform, exclusions, time, budget)

## Output

Write your assessment as:

```json
{
  "intent_alignment": "<aligned|drifted>",
  "complexity_growth": "<proportional|over_engineered>",
  "constraint_compliance": "<all_met|violated>",
  "detail": "<explanation if any field is not the ideal value>"
}
```

## Behavior

- If ALL three are ideal (aligned, proportional, all_met): return the JSON silently. The calling command will auto-continue.
- If ANY field indicates a problem: return the JSON with detailed explanation. The calling command will pause and present your findings to the user. **You do not make decisions — you only diagnose.**
```

- [ ] **Step 2: Test Supervisor with a drifted spec.json**

Create a test spec.json that's over-engineered for a simple task app (adds message queue, Redis, microservices). Run Supervisor against it with simple-app constraints.

Verify: Supervisor detects `complexity_growth: "over_engineered"` and explains the drift.

- [ ] **Step 3: Commit**

```bash
git add .claude/commands/supervisor.md
git commit -m "feat: Supervisor agent for global coherence checking"
```

---

## Task 8: Critic Agent

**Files:**
- Create: `.claude/commands/critic.md`

- [ ] **Step 1: Create critic.md**

```markdown
# Critic Agent — Independent Challenger

You are the Critic Agent for NoPilot. Your job is **independent quality verification** — checking that the current stage's output correctly satisfies upstream contracts.

You are a magnifying glass, not a telescope. You check each tree, not the forest shape (that's the Supervisor's job).

## CRITICAL: Independence

You run in an **independent session**. You have NO access to the conversation that generated the artifacts you're reviewing. You only see the final artifacts. This is by design — it prevents you from being biased by the generation process.

## Input

You receive only contract artifacts relevant to the current stage:
- For /discover review: specs/discover.json only
- For /spec review: specs/discover.json + specs/spec.json
- For /build review: specs/discover.json + specs/spec.json + specs/tests.json

## /spec Review Process (primary use case)

### Backward Verification

For EACH acceptance criterion in discover.json:
1. Find the module(s) in spec.json that should implement it
2. Trace through the module's interfaces and data models
3. Answer: "If implemented exactly per this spec, would this criterion be satisfied?"
4. If NO: record as uncovered criterion

### Invariant Verification

For EACH invariant in discover.json:
1. Check that no module design violates it
2. Check that the invariant is referenced in at least one module's invariant_refs

### Undeclared Core Behavior Check

Scan spec.json for any user-facing behavior (not technical behavior like pagination/error codes) that cannot be traced back to a requirement in discover.json.

## Output

Write to `specs/spec_review.json`:

```json
{
  "phase": "spec_review",
  "backward_verification": {
    "passed": true,
    "session": "independent",
    "uncovered_criteria": [],
    "invariant_violations": []
  },
  "undeclared_core_behaviors": [],
  "technical_behaviors_logged": [],
  "high_impact_decisions": [],
  "search_verification_results": [],
  "global_coherence_check": {}
}
```

Note: `global_coherence_check` is filled by the Supervisor agent, not by you. Leave it as empty object.

## Self-Fix Protocol

If you find issues:
1. Attempt to fix spec.json to align with discover.json
2. **You may ONLY modify the current stage's artifact (spec.json). You must NEVER modify upstream artifacts (discover.json).**
3. After fix, re-run your own verification
4. If fix succeeds: record what was fixed, mark passed
5. If fix fails: report the issue — the calling command will pause for user
```

- [ ] **Step 2: Test Critic with a misaligned spec.json**

Create a spec.json that's missing implementation for one requirement in discover.json. Run Critic.

Verify: Critic detects the uncovered criterion and either self-fixes or reports.

- [ ] **Step 3: Commit**

```bash
git add .claude/commands/critic.md
git commit -m "feat: Critic agent for independent backward verification"
```

---

## Task 9: End-to-End Integration Test

**Files:** No new files. Testing the full pipeline.

- [ ] **Step 1: Clean artifacts**

```bash
rm -f specs/*.json
```

- [ ] **Step 2: Run /discover**

Input: "I want to build a personal bookmark manager that saves URLs, adds tags, and lets me search by tag or full-text. Should work as a CLI tool."

Walk through:
- Step 0: tech_stack = "any", platform = "CLI", exclusions = "no web UI"
- Layer 1: select a direction
- Layer 2: confirm features + tech direction
- Layer 3: approve requirements

Verify: `specs/discover.json` and `specs/discover_history.json` exist with valid structure.

- [ ] **Step 3: Run /spec**

Run `/spec` (it reads discover.json automatically).

Verify:
- `specs/spec.json` exists with modules, interfaces, data models
- `specs/spec_review.json` exists with Critic + Supervisor results
- All requirement_refs trace back to valid REQ-xxx IDs
- All invariant_refs trace back to valid INV-xxx IDs

- [ ] **Step 4: Run /build**

Run `/build` (it reads spec.json + discover.json automatically).

Verify:
- `specs/tests.json` exists with example_cases + property_cases
- Coverage guards pass (no uncovered requirements/invariants)
- Code is generated via TDD
- `specs/build_report.json` exists with all fields populated
- Auto-acceptance ran against core scenarios

- [ ] **Step 5: Verify cross-stage traceability**

Check the full chain:
- discover.json REQ-001 → spec.json MOD-xxx requirement_refs → tests.json TEST-xxx requirement_refs
- discover.json INV-001 → spec.json MOD-xxx invariant_refs → tests.json PROP-xxx invariant_ref

All IDs should form a connected graph with no orphans.

- [ ] **Step 6: Commit integration test results**

```bash
git add specs/
git commit -m "test: end-to-end integration test artifacts (bookmark manager)"
```

---

## Self-Review Checklist

- [x] **Spec coverage:** All 10 sections of the spec are addressed: philosophy (embedded in each command), architecture (workflow.json), /discover (Tasks 2-4), /spec (Task 5), /build (Task 6), Supervisor (Task 7), Critic (Task 8), guardrails (embedded in commands), modes (Step 0 recommendation), evolution (V1 scope only)
- [x] **Placeholder scan:** No TBD/TODO. All code blocks contain actual content.
- [x] **Type consistency:** REQ-xxx, INV-xxx, MOD-xxx, SCENARIO-xxx, TEST-xxx, PROP-xxx naming consistent across all tasks.
- [x] **File paths consistent:** All references to specs/*.json, .claude/commands/*.md match the file structure.
